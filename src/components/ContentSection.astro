---
interface Props { id?: string; title: string; eyebrow?: string }
const { id, title, eyebrow } = Astro.props;
---
<section id={id} class="content-section page-width"><div>{eyebrow && <p class="eyebrow">{eyebrow}</p>}<h2>{title}</h2></div><div class="section-body"><slot /></div></section>
